# 旅游票务系统函数文档

## 1. 视图函数模块

### 1.1 首页相关函数

#### index()
- **功能**：处理网站首页的请求，展示轮播图、公告、最新资讯和热门景点
- **逻辑**：
  1. 从数据库中获取所有激活状态的轮播图，按轮播顺序排序
  2. 从数据库中获取所有标记为公告的资讯，按创建时间倒序排列，只取前3条
  3. 从数据库中获取所有非公告的旅游资讯，按创建时间倒序排列，只取前4条
  4. 从数据库中获取所有标记为热门的景点，按创建时间倒序排列，只取前6条
  5. 构建上下文字典，将查询到的数据传递给模板
  6. 渲染index.html模板，将上下文数据传递给模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的首页页面

### 1.2 用户认证相关函数

#### user_login()
- **功能**：处理用户登录请求
- **逻辑**：
  1. 获取next参数，用于登录后跳转回原页面
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取用户名、密码、角色和景点ID
  4. 根据用户名查找用户
  5. 验证密码
  6. 验证角色是否匹配
  7. 使用Django的login函数登录用户，创建会话
  8. 优先检查是否有next参数，如果有则跳转到该页面
  9. 检查会话中是否有购票选择，如果有则跳转到购票页面
  10. 根据用户角色重定向到不同页面
- **参数**：request（HTTP请求对象）
- **返回值**：登录成功后重定向到相应页面，失败则返回登录页面

#### user_logout()
- **功能**：处理用户退出请求
- **逻辑**：
  1. 使用Django的logout函数退出用户，清除会话
  2. 重定向到首页
- **参数**：request（HTTP请求对象）
- **返回值**：重定向到首页

#### forgot_password()
- **功能**：处理用户忘记密码请求
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取用户输入的邮箱、新密码和确认密码
  3. 验证密码是否一致
  4. 根据邮箱查找用户
  5. 更新密码
  6. 显示成功消息
  7. 重定向到登录页面
- **参数**：request（HTTP请求对象）
- **返回值**：重置密码成功后重定向到登录页面，失败则返回忘记密码页面

#### register()
- **功能**：处理用户注册请求
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 从POST请求中获取用户名、邮箱、密码、确认密码和角色
  3. 验证密码是否一致
  4. 验证用户名是否已存在
  5. 验证邮箱是否已存在
  6. 创建用户
  7. 设置用户角色
  8. 显示注册成功消息
  9. 重定向到登录页面
- **参数**：request（HTTP请求对象）
- **返回值**：注册成功后重定向到登录页面，失败则返回注册页面

### 1.3 景点管理员相关函数

#### scenic_admin_required()
- **功能**：景点管理员后台视图装饰器，需要登录且角色为景点管理员才能访问
- **逻辑**：
  1. 使用@login_required装饰器要求用户登录
  2. 检查用户角色是否为1（景点管理员）
  3. 如果是景点管理员，则执行视图函数
  4. 否则，显示错误消息并重定向到首页
- **参数**：view_func（视图函数）
- **返回值**：装饰后的视图函数

#### scenic_admin_scenic_spots()
- **功能**：景点信息管理视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取当前页码，默认为1
  3. 从数据库获取该管理员管理的景点
  4. 分页处理，每页显示10个景点
  5. 构建上下文数据
  6. 渲染scenic_admin/scenic_spots.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的景点管理页面

#### scenic_admin_edit_scenic_spot()
- **功能**：编辑景点视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 根据spot_id从数据库获取景点信息，确保该景点属于当前管理员
  3. 判断请求方法是否为POST（表单提交）
  4. 从POST请求中获取表单数据
  5. 表单验证
  6. 更新景点信息
  7. 处理图片上传
  8. 保存更新后的景点信息到数据库
  9. 显示成功消息
  10. 重定向到景点列表页面
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：编辑成功后重定向到景点列表页面，失败则返回编辑页面

#### scenic_admin_add_scenic_spot()
- **功能**：新增景点视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取当前景点管理员
  3. 从POST请求中获取表单数据
  4. 表单验证
  5. 验证价格和库存
  6. 创建新景点，关联到当前管理员
  7. 重新排序display_id
  8. 显示成功消息
  9. 重定向到景点列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到景点列表页面，失败则返回添加页面

#### scenic_admin_batch_operate_scenic_spots()
- **功能**：景点批量操作视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取操作类型
  3. 获取当前景点管理员
  4. 处理单个景点操作（上架、下架、删除）
  5. 处理批量操作（上架、下架、删除）
  6. 重新排序display_id（如果有删除操作）
  7. 显示成功消息
  8. 重定向到景点列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：操作完成后重定向到景点列表页面

#### scenic_admin_ticket_types()
- **功能**：门票类型管理视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取当前页码，默认为1
  3. 获取当前管理员管理的景点
  4. 获取当前管理员管理的景点的门票类型
  5. 分页处理，每页显示10个门票类型
  6. 获取选择的日期，如果没有或为空则使用当天日期
  7. 预处理日期库存数据
  8. 构建上下文数据
  9. 渲染scenic_admin/ticket_types.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的门票类型管理页面

#### scenic_admin_add_ticket_type()
- **功能**：新增门票类型视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取当前管理员管理的景点
  3. 判断请求方法是否为POST（表单提交）
  4. 获取表单数据
  5. 创建门票类型
  6. 如果有selected_date参数，同时为该日期创建库存记录
  7. 显示成功消息
  8. 重定向到门票类型列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到门票类型列表页面，失败则返回添加页面

#### scenic_admin_edit_ticket_type()
- **功能**：编辑门票类型视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取当前管理员管理的景点
  3. 获取要编辑的门票类型
  4. 判断请求方法是否为POST（表单提交）
  5. 获取表单数据
  6. 更新门票类型
  7. 如果有selected_date参数，同时更新该日期的库存记录
  8. 显示成功消息
  9. 重定向到门票类型列表页面
- **参数**：request（HTTP请求对象），ticket_id（门票类型ID）
- **返回值**：编辑成功后重定向到门票类型列表页面，失败则返回编辑页面

#### scenic_admin_batch_operate_ticket_types()
- **功能**：批量操作门票类型视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取操作类型和选中的门票类型ID
  3. 检查是否选择了门票类型
  4. 获取当前景点管理员
  5. 获取当前管理员管理的景点的门票类型
  6. 执行批量操作（上架、下架、删除）
  7. 显示成功消息
  8. 重定向到门票类型列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：操作完成后重定向到门票类型列表页面

#### scenic_admin_update_date_stock()
- **功能**：更新日期库存视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取门票类型ID、使用日期和库存数量
  3. 获取当前景点管理员
  4. 验证门票类型是否属于当前管理员
  5. 更新或创建DateStock记录
  6. 显示成功消息
  7. 重定向回门票类型列表页，保留选中的日期
- **参数**：request（HTTP请求对象）
- **返回值**：更新成功后重定向到门票类型列表页面

#### scenic_admin_orders()
- **功能**：订单管理视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 从数据库获取该管理员管理的景点
  3. 获取景点ID列表，用于后续查询
  4. 从请求中获取筛选条件
  5. 构建查询
  6. 根据状态筛选
  7. 根据搜索条件筛选
  8. 按创建时间倒序排序
  9. 分页处理，每页显示10条订单
  10. 构建上下文数据
  11. 渲染scenic_admin/orders.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的订单管理页面

#### scenic_admin_comments()
- **功能**：留言管理视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 从数据库获取该管理员管理的景点
  3. 获取景点ID列表，用于后续查询
  4. 从请求中获取筛选条件
  5. 构建查询
  6. 根据回复状态筛选
  7. 根据搜索条件筛选
  8. 按创建时间倒序排序
  9. 分页处理，每页显示10条留言
  10. 构建上下文数据
  11. 渲染scenic_admin/comments.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的留言管理页面

#### scenic_admin_reply_comment()
- **功能**：回复留言视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取回复内容
  3. 检查回复内容是否为空
  4. 获取当前景点管理员
  5. 获取要回复的留言
  6. 更新留言的回复信息
  7. 显示成功消息
  8. 重定向到留言管理页面
- **参数**：request（HTTP请求对象），comment_id（留言ID）
- **返回值**：回复成功后重定向到留言管理页面

#### scenic_admin_delete_comment()
- **功能**：删除留言视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取要删除的留言
  3. 删除留言
  4. 显示成功消息
  5. 重定向到留言管理页面
- **参数**：request（HTTP请求对象），comment_id（留言ID）
- **返回值**：删除成功后重定向到留言管理页面

#### scenic_admin_statistics()
- **功能**：数据统计视图
- **逻辑**：
  1. 导入日期处理模块
  2. 获取当前景点管理员
  3. 从数据库获取该管理员管理的景点
  4. 获取景点ID列表，用于后续查询
  5. 计算总订单数
  6. 计算总销售额
  7. 计算平均订单金额
  8. 计算总访问量
  9. 生成近7天的日期列表
  10. 计算每天的订单数和销售额
  11. 构建上下文数据
  12. 渲染scenic_admin/statistics.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的数据统计页面

#### scenic_admin_account()
- **功能**：账户信息管理视图
- **逻辑**：
  1. 获取当前景点管理员
  2. 判断请求方法是否为POST（表单提交）
  3. 更新用户信息
  4. 处理出生日期
  5. 处理头像上传
  6. 处理密码修改
  7. 保存用户信息
  8. 显示成功消息
  9. 重定向到账户信息页面
- **参数**：request（HTTP请求对象）
- **返回值**：更新成功后重定向到账户信息页面，失败则返回账户信息页面

#### scenic_admin_news_announcements()
- **功能**：资讯公告管理视图
- **逻辑**：
  1. 渲染scenic_admin/news_announcements.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的资讯公告管理页面

#### scenic_admin_announcements()
- **功能**：公告管理视图
- **逻辑**：
  1. 获取当前页码，默认为1
  2. 获取公告列表（is_announcement=True）
  3. 分页处理，每页显示10条公告
  4. 构建上下文数据
  5. 渲染scenic_admin/announcements.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的公告管理页面

#### scenic_admin_add_announcement()
- **功能**：新增公告视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取表单数据
  3. 创建公告
  4. 显示成功消息
  5. 重定向到公告列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到公告列表页面，失败则返回添加页面

#### scenic_admin_edit_announcement()
- **功能**：编辑公告视图
- **逻辑**：
  1. 获取要编辑的公告
  2. 判断请求方法是否为POST（表单提交）
  3. 获取表单数据
  4. 更新公告
  5. 显示成功消息
  6. 重定向到公告列表页面
- **参数**：request（HTTP请求对象），announcement_id（公告ID）
- **返回值**：编辑成功后重定向到公告列表页面，失败则返回编辑页面

#### scenic_admin_delete_announcement()
- **功能**：删除公告视图
- **逻辑**：
  1. 获取要删除的公告
  2. 删除公告
  3. 显示成功消息
  4. 重定向到公告列表页面
- **参数**：request（HTTP请求对象），announcement_id（公告ID）
- **返回值**：删除成功后重定向到公告列表页面

#### scenic_admin_batch_delete_announcements()
- **功能**：批量删除公告视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取选中的公告ID
  3. 检查是否选择了公告
  4. 删除选中的公告
  5. 显示成功消息
  6. 重定向到公告列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：删除成功后重定向到公告列表页面

#### scenic_admin_news()
- **功能**：资讯管理视图
- **逻辑**：
  1. 获取当前页码，默认为1
  2. 获取资讯列表（is_announcement=False）
  3. 分页处理，每页显示10条资讯
  4. 构建上下文数据
  5. 渲染scenic_admin/news.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的资讯管理页面

#### scenic_admin_add_news()
- **功能**：新增资讯视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取表单数据
  3. 创建资讯
  4. 显示成功消息
  5. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到资讯列表页面，失败则返回添加页面

#### scenic_admin_edit_news()
- **功能**：编辑资讯视图
- **逻辑**：
  1. 获取要编辑的资讯
  2. 判断请求方法是否为POST（表单提交）
  3. 获取表单数据
  4. 更新资讯
  5. 显示成功消息
  6. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象），news_id（资讯ID）
- **返回值**：编辑成功后重定向到资讯列表页面，失败则返回编辑页面

#### scenic_admin_delete_news()
- **功能**：删除资讯视图
- **逻辑**：
  1. 获取要删除的资讯
  2. 删除资讯
  3. 显示成功消息
  4. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象），news_id（资讯ID）
- **返回值**：删除成功后重定向到资讯列表页面

#### scenic_admin_batch_delete_news()
- **功能**：批量删除资讯视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取选中的资讯ID
  3. 检查是否选择了资讯
  4. 删除选中的资讯
  5. 显示成功消息
  6. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：删除成功后重定向到资讯列表页面

#### scenic_admin_refund_order()
- **功能**：景点管理员处理退款视图函数（审核通过）
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取当前景点管理员
  3. 获取该管理员管理的景点
  4. 获取订单信息，确保订单属于该管理员管理的景点
  5. 根据订单状态执行不同操作：
     - 已支付订单：设置为退款审核中
     - 退款审核中的订单：审核通过，设置为已退款，恢复对应日期的库存
  6. 显示成功消息
  7. 重定向到订单管理页面
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：处理成功后重定向到订单管理页面

#### scenic_admin_refund_reject()
- **功能**：景点管理员拒绝退款申请视图函数
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取当前景点管理员
  3. 获取该管理员管理的景点
  4. 获取订单信息，确保订单属于该管理员管理的景点
  5. 只有退款审核中的订单才能被拒绝
  6. 更新订单信息，拒绝退款，恢复为已支付状态
  7. 显示成功消息
  8. 重定向到订单管理页面
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：拒绝成功后重定向到订单管理页面

#### scenic_admin_order_detail()
- **功能**：景点管理员查看订单详情视图函数
- **逻辑**：
  1. 获取当前景点管理员
  2. 获取该管理员管理的景点
  3. 获取订单信息，确保订单属于该管理员管理的景点
  4. 构建上下文数据
  5. 渲染订单详情模板
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：渲染后的订单详情页面

### 1.4 平台管理员相关函数

#### admin_required()
- **功能**：平台管理员后台视图装饰器，需要登录且角色为平台管理员才能访问
- **逻辑**：
  1. 使用@login_required装饰器要求用户登录
  2. 检查用户角色是否为2（平台管理员）
  3. 如果是平台管理员，则执行视图函数
  4. 否则，显示错误消息并重定向到首页
- **参数**：view_func（视图函数）
- **返回值**：装饰后的视图函数

#### admin_index()
- **功能**：平台管理员控制台视图
- **逻辑**：
  1. 导入日期处理模块
  2. 从数据库获取统计数据：总用户数、总景点数、今日订单数、总销售额
  3. 获取最近订单：获取最新的2个订单
  4. 获取待处理留言：获取未回复的留言
  5. 构建上下文数据
  6. 渲染admin/index.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的平台管理员控制台页面

#### admin_user_list()
- **功能**：用户管理视图
- **逻辑**：
  1. 获取请求参数中的角色筛选条件
  2. 获取当前页码，默认为1
  3. 基础查询集
  4. 根据角色筛选用户
  5. 统计不同角色的用户数量
  6. 分页处理
  7. 构建上下文数据
  8. 渲染admin/user_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的用户管理页面

#### admin_add_user()
- **功能**：新增用户视图
- **逻辑**：
  1. 获取系统中的所有景点
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取表单数据
  4. 确保role有值，默认为'0'
  5. 表单验证
  6. 检查用户名是否已存在
  7. 检查邮箱是否已存在
  8. 计算下一个连续的用户ID
  9. 创建新用户，使用create_user方法会自动加密密码，并指定连续的ID
  10. 设置用户角色
  11. 如果是景点管理员，关联景点
  12. 显示成功消息
  13. 重定向到用户列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到用户列表页面，失败则返回添加页面

#### admin_delete_user()
- **功能**：删除用户视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 根据user_id从数据库获取用户
  3. 删除用户
  4. 显示成功消息
  5. 重定向到用户列表页面
- **参数**：request（HTTP请求对象），user_id（用户ID）
- **返回值**：删除成功后重定向到用户列表页面

#### admin_batch_delete_users()
- **功能**：批量删除用户视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取选中的用户ID列表
  3. 检查是否选择了用户
  4. 删除选中的用户
  5. 显示成功消息
  6. 构建重定向URL，保持筛选条件
  7. 重定向到用户列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：删除成功后重定向到用户列表页面

#### admin_edit_user()
- **功能**：编辑用户视图
- **逻辑**：
  1. 根据user_id从数据库获取用户信息
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取表单数据
  4. 表单验证
  5. 检查用户名是否已被其他用户使用
  6. 检查邮箱是否已被其他用户使用
  7. 处理密码修改
  8. 更新用户信息
  9. 处理出生日期
  10. 处理头像上传
  11. 保存更新后的用户信息到数据库
  12. 显示成功消息
  13. 重定向到用户列表页面
- **参数**：request（HTTP请求对象），user_id（用户ID）
- **返回值**：编辑成功后重定向到用户列表页面，失败则返回编辑页面

#### admin_category_list()
- **功能**：景点分类管理视图
- **逻辑**：
  1. 获取请求参数中的搜索关键词
  2. 获取当前页码，默认为1
  3. 基础查询集
  4. 根据搜索关键词筛选分类
  5. 分页处理，每页显示10个分类
  6. 构建上下文数据
  7. 渲染admin/category_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的景点分类管理页面

#### admin_add_category()
- **功能**：添加景点分类视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取表单数据
  3. 表单验证
  4. 计算下一个连续的分类ID
  5. 创建分类，指定连续的ID
  6. 显示成功消息
  7. 重定向到分类列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到分类列表页面，失败则返回添加页面

#### admin_edit_category()
- **功能**：编辑景点分类视图
- **逻辑**：
  1. 获取要编辑的分类
  2. 判断请求方法是否为POST（表单提交）
  3. 获取表单数据
  4. 表单验证
  5. 更新分类
  6. 显示成功消息
  7. 重定向到分类列表页面
- **参数**：request（HTTP请求对象），category_id（分类ID）
- **返回值**：编辑成功后重定向到分类列表页面，失败则返回编辑页面

#### admin_delete_category()
- **功能**：删除景点分类视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取要删除的分类
  3. 删除分类
  4. 显示成功消息
  5. 重定向到分类列表页面
- **参数**：request（HTTP请求对象），category_id（分类ID）
- **返回值**：删除成功后重定向到分类列表页面

#### admin_scenic_list()
- **功能**：景点管理视图
- **逻辑**：
  1. 获取请求参数中的搜索关键词
  2. 获取当前页码，默认为1
  3. 基础查询集
  4. 根据搜索关键词筛选景点
  5. 统计不同状态的景点数量
  6. 获取所有地区（用于筛选）
  7. 景点分类选项：从Category模型获取所有分类
  8. 分页处理
  9. 构建上下文数据
  10. 渲染admin/scenic_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的景点管理页面

#### admin_region_list()
- **功能**：地区分类管理视图
- **逻辑**：
  1. 获取请求参数中的搜索关键词
  2. 获取当前页码，默认为1
  3. 基础查询集，按ID大小排序
  4. 根据搜索关键词筛选地区
  5. 分页处理，每页显示10个地区
  6. 构建上下文数据
  7. 渲染admin/region_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的地区分类管理页面

#### admin_add_region()
- **功能**：添加地区分类视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取表单数据
  3. 表单验证
  4. 计算下一个连续的地区ID
  5. 创建地区，指定连续的ID
  6. 显示成功消息
  7. 重定向到地区列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到地区列表页面，失败则返回添加页面

#### admin_edit_region()
- **功能**：编辑地区分类视图
- **逻辑**：
  1. 获取要编辑的地区
  2. 判断请求方法是否为POST（表单提交）
  3. 获取表单数据
  4. 表单验证
  5. 更新地区信息
  6. 显示成功消息
  7. 重定向到地区列表页面
- **参数**：request（HTTP请求对象），region_id（地区ID）
- **返回值**：编辑成功后重定向到地区列表页面，失败则返回编辑页面

#### admin_delete_region()
- **功能**：删除地区分类视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取要删除的地区
  3. 删除地区
  4. 显示成功消息
  5. 重定向到地区列表页面
- **参数**：request（HTTP请求对象），region_id（地区ID）
- **返回值**：删除成功后重定向到地区列表页面

#### reorder_scenic_display_ids()
- **功能**：重新排序景点显示ID的辅助函数
- **逻辑**：
  1. 按id升序获取所有景点
  2. 重新分配display_id
- **参数**：无
- **返回值**：无

#### admin_delete_scenic()
- **功能**：删除景点视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 根据spot_id从数据库获取景点
  3. 删除景点
  4. 重新排序display_id
  5. 显示成功消息
  6. 获取搜索关键词，保持搜索状态
  7. 重定向到景点列表页面
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：删除成功后重定向到景点列表页面

#### admin_batch_delete_scenic()
- **功能**：批量删除景点视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取选中的景点ID列表
  3. 检查是否选择了景点
  4. 删除选中的景点
  5. 重新排序display_id
  6. 显示成功消息
  7. 构建重定向URL，保持搜索条件
  8. 重定向到景点列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：删除成功后重定向到景点列表页面

#### admin_add_scenic()
- **功能**：新增景点视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 从POST请求中获取表单数据
  3. 表单验证
  4. 创建新景点
  5. 重新排序display_id
  6. 显示成功消息
  7. 重定向到景点列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到景点列表页面，失败则返回添加页面

#### admin_edit_scenic()
- **功能**：编辑景点视图
- **逻辑**：
  1. 根据spot_id从数据库获取景点信息
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取表单数据
  4. 表单验证
  5. 更新景点信息
  6. 处理图片上传
  7. 保存更新后的景点信息到数据库
  8. 显示成功消息
  9. 重定向到景点列表页面
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：编辑成功后重定向到景点列表页面，失败则返回编辑页面

#### admin_order_list()
- **功能**：订单管理视图
- **逻辑**：
  1. 获取请求参数中的订单状态筛选条件
  2. 获取请求参数中的搜索关键词
  3. 获取当前页码，默认为1
  4. 基础查询集：包含关联的用户和景点信息，使用select_related优化查询
  5. 根据订单状态筛选
  6. 根据搜索关键词筛选：可搜索订单号、用户名、用户邮箱或景点名称
  7. 按创建时间倒序排序
  8. 统计不同状态的订单数量
  9. 订单状态选项
  10. 分页处理
  11. 构建上下文数据
  12. 渲染admin/order_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的订单管理页面

#### admin_delete_order()
- **功能**：删除订单视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 根据order_id从数据库获取订单
  3. 删除订单
  4. 显示成功消息
  5. 获取筛选条件，保持筛选状态
  6. 构建重定向URL
  7. 重定向到订单列表页面
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：删除成功后重定向到订单列表页面

#### admin_comment_list()
- **功能**：留言管理视图
- **逻辑**：
  1. 获取请求参数中的回复状态筛选条件
  2. 获取请求参数中的搜索关键词
  3. 获取当前页码，默认为1
  4. 基础查询集：包含关联的用户和景点信息，使用select_related优化查询
  5. 根据回复状态筛选
  6. 根据搜索关键词筛选：可搜索用户名或留言内容
  7. 统计不同回复状态的留言数量
  8. 分页处理
  9. 构建上下文数据
  10. 渲染admin/comment_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的留言管理页面

#### admin_reply_comment()
- **功能**：管理员回复留言视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取要回复的留言
  3. 获取回复内容
  4. 检查回复内容是否为空
  5. 更新留言的回复信息
  6. 显示成功消息
  7. 重定向回留言管理页面
- **参数**：request（HTTP请求对象），comment_id（留言ID）
- **返回值**：回复成功后重定向到留言管理页面

#### admin_delete_comment()
- **功能**：管理员删除留言视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取要删除的留言
  3. 删除留言
  4. 显示成功消息
  5. 重定向回留言管理页面
- **参数**：request（HTTP请求对象），comment_id（留言ID）
- **返回值**：删除成功后重定向到留言管理页面

#### admin_announcement_list()
- **功能**：公告管理视图
- **逻辑**：
  1. 获取请求参数中的搜索关键词
  2. 获取当前页码，默认为1
  3. 基础查询集：只获取公告类型的资讯，按创建时间倒序排列
  4. 根据搜索关键词筛选：可搜索公告标题或内容
  5. 统计公告数量
  6. 分页处理
  7. 构建上下文数据
  8. 渲染admin/announcement_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的公告管理页面

#### admin_add_announcement()
- **功能**：新增公告视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 从POST请求中获取表单数据
  3. 表单验证
  4. 创建新公告
  5. 显示成功消息
  6. 重定向到公告列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到公告列表页面，失败则返回添加页面

#### admin_edit_announcement()
- **功能**：编辑公告视图
- **逻辑**：
  1. 获取要编辑的公告
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取表单数据
  4. 表单验证
  5. 更新公告信息
  6. 显示成功消息
  7. 重定向到公告列表页面
- **参数**：request（HTTP请求对象），announcement_id（公告ID）
- **返回值**：编辑成功后重定向到公告列表页面，失败则返回编辑页面

#### admin_delete_announcement()
- **功能**：删除公告视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 根据announcement_id从数据库获取公告
  3. 删除公告
  4. 显示成功消息
  5. 获取搜索关键词，保持搜索状态
  6. 重定向到公告列表页面
- **参数**：request（HTTP请求对象），announcement_id（公告ID）
- **返回值**：删除成功后重定向到公告列表页面

#### admin_news_list()
- **功能**：资讯管理视图
- **逻辑**：
  1. 获取请求参数中的搜索关键词
  2. 获取当前页码，默认为1
  3. 基础查询集：只获取非公告类型的资讯，按创建时间倒序排列
  4. 根据搜索关键词筛选：可搜索资讯标题或内容
  5. 统计资讯数量
  6. 分页处理
  7. 构建上下文数据
  8. 渲染admin/news_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的资讯管理页面

#### admin_add_news()
- **功能**：新增资讯视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 从POST请求中获取表单数据
  3. 表单验证
  4. 创建新资讯
  5. 显示成功消息
  6. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象）
- **返回值**：添加成功后重定向到资讯列表页面，失败则返回添加页面

#### admin_edit_news()
- **功能**：编辑资讯视图
- **逻辑**：
  1. 获取要编辑的资讯
  2. 判断请求方法是否为POST（表单提交）
  3. 从POST请求中获取表单数据
  4. 表单验证
  5. 更新资讯信息
  6. 显示成功消息
  7. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象），news_id（资讯ID）
- **返回值**：编辑成功后重定向到资讯列表页面，失败则返回编辑页面

#### admin_delete_news()
- **功能**：删除资讯视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 根据news_id从数据库获取资讯
  3. 删除资讯
  4. 显示成功消息
  5. 获取搜索关键词，保持搜索状态
  6. 重定向到资讯列表页面
- **参数**：request（HTTP请求对象），news_id（资讯ID）
- **返回值**：删除成功后重定向到资讯列表页面

#### admin_password_change()
- **功能**：密码管理视图
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 从POST请求中获取表单数据
  3. 表单验证
  4. 检查新密码是否一致
  5. 获取当前登录用户
  6. 验证旧密码
  7. 更新密码
  8. 显示成功消息
  9. 退出登录
  10. 重定向到登录页面
- **参数**：request（HTTP请求对象）
- **返回值**：修改成功后重定向到登录页面，失败则返回密码修改页面

### 1.5 普通用户相关函数

#### personal_center()
- **功能**：个人中心视图函数，处理用户个人中心请求
- **逻辑**：
  1. 获取当前登录用户
  2. 判断请求方法是否为POST（表单提交）
  3. 更新用户信息
  4. 处理出生日期
  5. 处理头像上传
  6. 保存用户信息
  7. 显示成功消息
  8. 重定向到个人中心页面
  9. 获取用户收藏的景点
  10. 获取用户的浏览历史
  11. 获取用户的评论
  12. 构建上下文数据
  13. 渲染personal_center.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的个人中心页面

#### change_password()
- **功能**：修改密码视图函数，需要登录才能访问
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取表单数据
  3. 获取当前登录用户
  4. 验证原密码
  5. 验证新密码
  6. 更新密码
  7. 保存用户信息
  8. 显示成功消息
  9. 重定向到个人中心页面
- **参数**：request（HTTP请求对象）
- **返回值**：修改成功后重定向到个人中心页面

#### scenic_spots()
- **功能**：景点信息列表视图
- **逻辑**：
  1. 获取搜索关键词
  2. 获取地区筛选条件
  3. 获取分类筛选条件
  4. 获取当前页码，默认为1
  5. 基础查询集
  6. 根据搜索关键词筛选
  7. 根据地区筛选
  8. 根据分类筛选
  9. 获取所有地区（用于筛选），只显示省份数据
  10. 获取所有分类（用于筛选）
  11. 分页处理
  12. 预处理分页后的景点数据，将tags转换为列表
  13. 构建上下文
  14. 渲染scenic_spots.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的景点列表页面

#### scenic_spot_detail()
- **功能**：景点详情视图函数，处理单个景点的详情页请求
- **逻辑**：
  1. 根据景点ID从数据库中获取景点信息
  2. 将tags字符串分割为列表
  3. 获取该景点的所有留言，按创建时间倒序排列
  4. 记录浏览历史（如果用户已登录）
  5. 处理留言提交（如果用户已登录且请求方法为POST）
  6. 构建上下文
  7. 渲染scenic_spot_detail.html模板
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：渲染后的景点详情页面

#### news_list()
- **功能**：资讯公告列表视图函数，处理资讯公告列表页请求
- **逻辑**：
  1. 获取筛选参数，默认显示全部
  2. 获取当前页码，默认为1
  3. 根据筛选条件获取资讯公告，按创建时间倒序排列
  4. 分页处理
  5. 渲染news_list.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的资讯公告列表页面

#### news_detail()
- **功能**：资讯公告详情视图函数，处理单个资讯公告的详情页请求
- **逻辑**：
  1. 根据资讯ID从数据库中获取单个资讯公告信息
  2. 渲染news_detail.html模板
- **参数**：request（HTTP请求对象），news_id（资讯ID）
- **返回值**：渲染后的资讯公告详情页面

#### buy_ticket()
- **功能**：购票页面视图函数，处理景点购票请求
- **逻辑**：
  1. 导入日期处理模块
  2. 获取当前日期
  3. 根据景点ID从数据库中获取景点信息
  4. 获取该景点的所有激活状态的门票类型
  5. 分类门票类型：单票和套票
  6. 检查会话中是否有购票选择
  7. 处理表单提交
  8. 验证是否选择了门票类型
  9. 获取门票类型
  10. 检查当天的库存是否充足
  11. 计算总价
  12. 处理不同的操作：直接购买或加入购物车
  13. 获取当前选择的日期，如果没有则使用今天
  14. 为每个门票类型获取对应日期的库存数据
  15. 渲染buy_ticket.html模板
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：渲染后的购票页面，或重定向到支付页面/购物车页面/登录页面

#### weather_check()
- **功能**：天气页面视图函数，显示门票使用日期的天气情况
- **逻辑**：
  1. 根据订单ID从数据库中获取订单信息
  2. 获取景点地区名称
  3. 调用天气API获取真实天气数据
  4. 如果天气查询失败，使用默认模拟数据
  5. 渲染weather_check.html模板
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：渲染后的天气查询页面

#### get_ticket_stocks()
- **功能**：AJAX获取门票库存视图函数
- **逻辑**：
  1. 获取请求参数
  2. 检查参数是否完整
  3. 解析日期字符串为date对象
  4. 获取景点信息
  5. 获取该景点的所有激活状态的门票类型
  6. 构建库存数据
  7. 返回JSON响应
- **参数**：request（HTTP请求对象）
- **返回值**：JSON格式的库存数据

#### payment()
- **功能**：支付页面视图函数，处理订单支付请求
- **逻辑**：
  1. 根据订单ID从数据库中获取订单信息
  2. 渲染通用支付页面，将订单数据传递给模板
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：渲染后的支付页面

#### cart()
- **功能**：购物车视图函数，处理用户购物车请求
- **逻辑**：
  1. 获取当前登录用户的购物车记录
  2. 为每个购物车项添加总价属性
  3. 处理表单提交（购买功能）
  4. 获取用户选择的购物车项目ID
  5. 检查是否选择了商品
  6. 遍历选中的购物车项目，创建订单
  7. 去重处理，避免重复处理同一个购物车项
  8. 生成订单号
  9. 检查库存是否充足
  10. 计算订单总价
  11. 创建订单
  12. 减少库存
  13. 从购物车中删除已购买的项目
  14. 如果只有一个订单，直接跳转到支付页面
  15. 多个订单，将订单ID存储到会话中，然后跳转到批量支付页面
  16. 渲染cart.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的购物车页面，或重定向到支付页面/批量支付页面

#### order_center()
- **功能**：订单中心视图函数，处理用户订单中心请求
- **逻辑**：
  1. 获取当前登录用户的所有订单
  2. 获取当前页码，默认为1
  3. 分页处理
  4. 渲染order_center.html模板
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的订单中心页面

#### delete_cart_item()
- **功能**：删除购物车项视图函数
- **逻辑**：
  1. 判断请求方法是否为POST
  2. 获取购物车项，确保属于当前用户
  3. 删除购物车项
  4. 跳转到购物车页面
- **参数**：request（HTTP请求对象），cart_id（购物车项ID）
- **返回值**：删除成功后重定向到购物车页面

#### add_to_collection()
- **功能**：移入收藏视图函数
- **逻辑**：
  1. 判断请求方法是否为POST
  2. 获取购物车项，确保属于当前用户
  3. 创建或获取收藏记录
  4. 从购物车中删除该项目
  5. 如果是新创建的收藏，显示成功信息
  6. 跳转到购物车页面
- **参数**：request（HTTP请求对象），cart_id（购物车项ID）
- **返回值**：操作成功后重定向到购物车页面

#### batch_payment()
- **功能**：批量支付视图函数，显示所有选中的订单并计算总价格
- **逻辑**：
  1. 获取订单ID列表（从会话或请求参数中获取）
  2. 检查是否有订单ID
  3. 获取订单信息
  4. 计算总价格
  5. 渲染通用支付页面
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的批量支付页面

#### confirm_payment()
- **功能**：确认支付视图函数，处理实际的支付逻辑
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取选中的订单ID列表
  3. 检查是否选择了订单
  4. 获取支付方式
  5. 统计成功和失败的订单数量
  6. 遍历处理每个订单
  7. 获取订单信息，确保订单属于当前用户
  8. 只有待支付订单才能支付
  9. 模拟支付成功
  10. 更新订单状态为已支付
  11. 更新支付相关字段
  12. 生成支付流水号
  13. 减少对应日期的库存
  14. 显示结果消息
  15. 如果有成功支付的订单，跳转到第一个订单的天气页面
  16. 否则跳转到订单中心
- **参数**：request（HTTP请求对象）
- **返回值**：支付成功后重定向到天气页面/订单中心

#### cancel_order()
- **功能**：取消订单视图函数，处理用户取消订单请求
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取订单信息，确保订单属于当前用户
  3. 只有待支付订单才能取消
  4. 恢复对应日期的库存
  5. 更新订单状态为已取消
  6. 显示成功消息
  7. 重定向回订单中心页面
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：取消成功后重定向到订单中心页面

#### apply_refund()
- **功能**：申请退款视图函数，处理用户申请退款请求
- **逻辑**：
  1. 判断请求方法是否为POST（表单提交）
  2. 获取退款原因
  3. 获取订单信息，确保订单属于当前用户
  4. 只有已支付订单才能申请退款
  5. 更新订单信息
  6. 设置为退款审核中，等待管理员审核
  7. 显示成功消息
  8. 重定向到订单中心页面
- **参数**：request（HTTP请求对象），order_id（订单ID）
- **返回值**：申请成功后重定向到订单中心页面

#### contact_service()
- **功能**：联系客服视图函数，处理用户联系客服请求
- **逻辑**：
  1. 渲染contact_service.html模板，显示联系客服页面
- **参数**：request（HTTP请求对象）
- **返回值**：渲染后的联系客服页面

#### add_favorite()
- **功能**：收藏景点视图函数
- **逻辑**：
  1. 判断请求方法是否为POST
  2. 获取景点信息
  3. 创建或获取收藏记录
  4. 如果已收藏，则删除收藏记录
  5. 返回JSON响应
- **参数**：request（HTTP请求对象），spot_id（景点ID）
- **返回值**：JSON格式的操作结果

### 1.6 API相关函数

#### get_scenic_spots_api()
- **功能**：获取景点列表API视图函数，用于登录页面的景点选择下拉框
- **逻辑**：
  1. 获取所有景点
  2. 构建景点列表数据
  3. 返回JSON响应
- **参数**：request（HTTP请求对象）
- **返回值**：JSON格式的景点列表数据

## 2. 天气API模块

### 2.1 天气查询函数

#### get_weather_by_region()
- **功能**：根据地区名称和日期获取天气信息
- **逻辑**：
  1. 记录日志
  2. 将目标日期转换为字符串格式
  3. 根据API模式选择使用真实API或模拟数据
  4. 调用相应的函数获取天气信息
- **参数**：region_name（地区名称），target_date（目标日期，date对象）
- **返回值**：天气信息字典

#### _get_real_weather()
- **功能**：使用真实API获取天气信息
- **逻辑**：
  1. 记录日志
  2. 检查API密钥是否已配置
  3. 构建请求参数
  4. 尝试多次请求，增加可靠性
  5. 发送请求
  6. 处理响应数据
  7. 构建天气信息
  8. 处理各种异常情况
- **参数**：region_name（地区名称），target_date_str（目标日期字符串）
- **返回值**：天气信息字典

#### _get_mock_weather()
- **功能**：使用模拟数据获取天气信息
- **逻辑**：
  1. 记录日志
  2. 获取对应地区的天气数据，如果没有则使用默认数据
  3. 获取目标日期的天气数据，如果没有则使用当天数据
  4. 构建天气信息
- **参数**：region_name（地区名称），target_date_str（目标日期字符串）
- **返回值**：天气信息字典

#### get_weather_advice()
- **功能**：根据天气和温度生成出行建议
- **逻辑**：
  1. 根据天气状况生成不同的出行建议
  2. 如果是晴天，根据温度生成不同的建议
  3. 处理各种天气情况
- **参数**：weather（天气状况），temperature（温度）
- **返回值**：出行建议字符串

## 3. 管理命令模块

### 3.1 数据处理命令

#### check_tags.py
- **功能**：检查景点标签
- **逻辑**：
  1. 检查景点标签的格式和内容
- **参数**：无
- **返回值**：无

#### clean_tags.py
- **功能**：清理景点标签
- **逻辑**：
  1. 清理景点标签中的无效内容
- **参数**：无
- **返回值**：无

#### transfer_scenicspot_data.py
- **功能**：转移景点数据
- **逻辑**：
  1. 转移景点数据到新的结构
- **参数**：无
- **返回值**：无

#### update_content.py
- **功能**：更新内容
- **逻辑**：
  1. 更新系统中的内容
- **参数**：无
- **返回值**：无

#### update_regions.py
- **功能**：更新地区信息
- **逻辑**：
  1. 更新系统中的地区信息
- **参数**：无
- **返回值**：无

## 4. 总结

本系统包含了以下主要模块的函数：

1. **视图函数模块**：包含了网站的所有页面视图，包括首页、用户认证、景点管理、订单管理、个人中心等功能。

2. **天气API模块**：包含了天气查询相关的函数，支持使用真实API和模拟数据获取天气信息。

3. **管理命令模块**：包含了数据处理相关的管理命令，用于维护系统数据。

这些函数共同构成了一个完整的旅游票务系统，实现了景点管理、门票销售、订单管理、用户管理等功能。系统采用了Django框架，使用了MVC架构模式，代码结构清晰，功能完整。